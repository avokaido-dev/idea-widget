/**
 * The package entry, for anyone who bundles their frontend.
 *
 * If you are pasting a `<script>` tag instead, you want `dist/v1.js` (or the
 * hosted copy of it) — that build calls `createIdeaWidget` for you from the
 * tag's `data-*` attributes.
 */

export { CORNERS, DEFAULT_ORIGIN, createIdeaWidget } from "./widget.js";
